name: Test limit access to actor
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          upterm-server: "ssh://uptermd.upterm.dev:22"
          detached: "true"
          wait-timeout-minutes: "1"
          limit-access-to-actor: "true"
      - name: Assert action ran with limit-access-to-actor or failed gracefully
        env:
          OUTCOME: ${{ steps.action.outcome }}
          SSH_CMD: ${{ steps.action.outputs.ssh-command }}
        run: |
          echo "outcome: $OUTCOME"
          echo "ssh-command: $SSH_CMD"
          if [ "$OUTCOME" = "success" ]; then
            if [ -z "$SSH_CMD" ]; then
              echo "FAIL: action succeeded but ssh-command output is empty"
              exit 1
            fi
            echo "PASS: got ssh-command '$SSH_CMD'"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (network/server not reachable or actor has no SSH keys)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
